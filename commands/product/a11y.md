---
description: Accessibility audit — WCAG 2.1 AA compliance, keyboard navigation, screen reader support, color contrast
argument-hint: "<target> [--level A|AA|AAA] [--quick]"
phase_relevance: ["clarify", "design", "review"]
archetype_relevance: ["*"]
---

# /wicked-garden:product:a11y

Accessibility audit for UI code/components: WCAG 2.1 AA, keyboard nav, screen
reader support, color contrast, semantic structure.

## Run it inline (no dispatch)

1. Parse `<target>`, `--level` (default AA), `--quick`.
2. `Read("${CLAUDE_PLUGIN_ROOT}/skills/product/refs/a11y.md")` — the POUR rubric, checklist, common violations, and output format.
3. Read the target file(s), apply the rubric directly, and emit the audit. For deeper WCAG/ARIA/keyboard/screen-reader detail, load the `accessibility` skill (`skills/product/accessibility/`).

Track remediation via `TaskCreate`/`TaskUpdate` (`metadata.event_type="task"`); pair with `product:ux-review` for visual consistency.
