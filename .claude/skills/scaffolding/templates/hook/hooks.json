{
  "description": "{{description}}",
  "hooks": {
    "{{event}}": [
      {
        "matcher": "{{matcher}}",
        "hooks": [
          {
            "type": "command",
            "command": "python ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/{{script_name}}.py",
            "timeout": 10,
            "priority": 100
          }
        ]
      }
    ]
  }
}
