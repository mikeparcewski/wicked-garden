{
  "$schema": "wicked-data/1.0.0",
  "api_script": "scripts/api.py",
  "sources": [
    {
      "name": "metrics",
      "description": "Computed delivery metrics (throughput, cycle time, backlog health, completion rate, optional cost/ROI) from kanban task data",
      "capabilities": [
        "stats"
      ]
    },
    {
      "name": "commentary",
      "description": "Rule-based delivery insights from metric deltas, auto-refreshed on task changes",
      "capabilities": [
        "list"
      ]
    }
  ]
}
