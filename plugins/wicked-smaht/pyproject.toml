[project]
name = "wicked-smaht"
version = "2.0.0"
description = "Intelligent context management for Claude Code"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
