Verifies the unknown-event survival invariant (locked decision #8). A known `wicked.project.created` event lands first, then an entirely unknown `wicked.totally.fake_event` is emitted midstream, followed by a known `wicked.gate.decided` and `wicked.phase.transitioned`. The projector must not raise on the unknown event; it must record the event in `event_log` with `projection_status=ignored`; and the cursor must advance past event_id 2. The final projection is identical to the `single_phase_approve` shape for `zeta-lib`, proving that the unknown event neither corrupts state nor stalls the stream. This fixture directly tests locked decision #8 from the architecture contract.
