Minimal happy-path fixture: a single project is created via `wicked.project.created`, then its `clarify` phase receives an APPROVE gate verdict via `wicked.gate.decided` and advances via `wicked.phase.transitioned`. The expected projection is one project row (`status=active`, `current_phase=clarify`) and one phase row (`state=approved`, `gate_verdict=APPROVE`, `gate_score=0.85`). Timestamps in expected files use sentinel `0` so the comparator wildcards them. This fixture verifies the end-to-end create→gate→transition flow with no rework or complications.
