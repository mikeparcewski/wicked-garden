Tests the REJECT → rework path. A project is created, then the `design` phase gate returns REJECT (score 0.45). The companion `wicked.gate.blocked` event fires, followed by `wicked.rework.triggered` carrying `iteration_count=1`. The expected projection is a phase row with `state=rejected`, `gate_verdict=REJECT`, and `rework_iterations=1`. The project row retains `current_phase=""` because no phase transition has been approved yet. This fixture locks the invariant that a REJECT sets phase `state=rejected` and `terminal_at`, while `rework.triggered` increments `rework_iterations` via last-write-wins semantics on the payload's `iteration_count` field.
